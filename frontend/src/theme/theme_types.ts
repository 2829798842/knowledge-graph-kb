/**
 * 模块名称：theme/theme_types
 * 主要功能：定义前端主题模式与解析后的主题类型。
 */

/**
 * 主题模式。
 */
export type ThemeMode = 'system' | 'light' | 'dark';

/**
 * 解析后的实际主题。
 */
export type ResolvedTheme = 'light' | 'dark';
